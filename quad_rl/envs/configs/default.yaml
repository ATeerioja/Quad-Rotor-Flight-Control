# Backward-compatible alias for hover.yaml (kept so `config_path=None` /
# any existing "default.yaml" reference keeps resolving to the full
# hover-task config: default.yaml -> hover.yaml -> base.yaml).
defaults:
  - hover.yaml
